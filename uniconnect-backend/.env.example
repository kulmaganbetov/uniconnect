DATABASE_URL=postgres://postgres:password@localhost:5432/uniconnect?sslmode=disable
JWT_SECRET=your_secret_key
PORT=8080

# Optional — enables the AI Consultant page. Without this the
# /api/ai/chat endpoint returns a 503 with a friendly message.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
